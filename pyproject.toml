[project]
name = "applicant-zero"
version = "0.1.0"
description = "Private job discovery and application preparation prototype"
requires-python = ">=3.11"

[project.optional-dependencies]
browser = ["playwright>=1.51,<2"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
